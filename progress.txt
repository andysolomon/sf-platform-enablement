Platform Enablement Layer - Salesforce Integration & Reusable Components - Progress
Generated: 2026-06-16 (project #2, built in the same spirit as projects/todo-app/)
Plan: docs/IMPLEMENTATION_PLAN.md
Thesis: todo-app was an APP TEAM; this is the PLATFORM TEAM that serves it.
        The reusable layer is the product; the integration is the proof.
JD focus: the two "Must have" lines todo-app left thin -
          (1) Salesforce integration patterns  (2) reusable platform enablers / explicit APIs.
Reuses: todo-app orgs (DevHub orgfarm-60a172912c / prod orgfarm-5b96907065), the Phase-9
        reusable CI template, glab/auth + base64-secret + scratch-budget lessons.
Constraint: 6 scratch-org creates/day SHARED with any todo-app work - count before pushing.
            Package-version builds are a SEPARATE 6/day limit. API pinned 67.0 (Summer '26).

[x] 1.0 - Phase 1 - Governance and Platform Charter  *** PHASE 1 COMPLETE 2026-06-17 (back-filled after Phase 2) ***
    [x] 1.1 - docs/governance/charter.md (platform-as-product; roles incl. Consumer App Lead + InfoSec)
              2026-06-17 DONE. Reuses todo-app hats/SoD/ALM-stages; ADDS Consumer App Lead hat (signs off public-API
              changes, veto on breaking w/o migration) + InfoSec hat (signs off trust boundary/auth/secrets). Decision-
              authority table adds public-API-change + security-config rows. Maturity: Practicing -> Leading via reuse.
    [x] 1.2 - docs/governance/api-governance.md (public-API definition, semver, DEPRECATION policy) <- the differentiator
              2026-06-17 DONE. THE net-new doc (todo-app had no analogue). Defines public surface (in public-api-spec.md
              or integration-contract.md = public; else internal/free-to-change); semver of the CONTRACT (additive=minor,
              breaking=major); 4-step deprecation cycle (deprecate->warn->notify->remove, >=1 minor window); change
              workflow needs Architect+Consumer-Lead sign-off + spec updated same MR. Constrains NFR-8.
    [x] 1.3 - docs/governance/change-management.md (adds the breaking-API change class)
              2026-06-17 DONE. Reuses todo-app classes + ADDS "Breaking API change" class (Architect+Consumer Lead+CCB,
              must follow deprecation cycle). Additive-only now covers public-API too (NFR-11). Security/auth change needs
              InfoSec sign-off. Sharper direct-prod-change warning: manual base change desyncs installed consumer.
    [x] 1.4 - docs/governance/definition-of-done.md (adds public-API doc + consumer-impact)
              2026-06-17 DONE. Extends todo-app DoD: no endpoint/secret in source; callouts async (no callout-after-DML);
              HttpCalloutMock incl timeout/4xx/5xx/malformed; inbound idempotency proven; contract test vs deployed mock;
              public-api-spec/integration-contract updated same MR; consumer-impact + deprecation-log; Consumer/InfoSec sign-offs.
    [x] 1.5 - docs/decision-log/ADR-001-packaging-model.md (base + extension package vs monolith)
              2026-06-17 DONE. Two-tier unlocked pkg (base <- todo-app consumer #1 + stub consumer #2), no namespace.
              EXPLICITLY picks up todo-app ADR-003's deferred "split only with a real dependency motive (new ADR)" -
              THIS is that motive. Dependency-ordered install; rollback rehearsed WITH consumer installed (Phase 8);
              additive-only API/schema load-bearing for NFR-8/NFR-11. Supersedes ADR-003 deferral for platform context only.
    NOTE: Phase 1 was back-filled AFTER Phase 2 (Andy directed requirements-first). All later phases that lean on
    api-governance.md (NFR-8, RTM, change-mgmt breaking-API class) now have their dependency in place.
[x] 2.0 - Phase 2 - Requirements (Platform Capabilities + Integration)  *** PHASE 2 COMPLETE 2026-06-17 ***
    [x] 2.0a- docs/requirements/system-landscape.md (deck DISCOVERY: current-state inventory, SoR, pain points, integration decision framework applied)
              2026-06-16 DONE. Grounded in 11 integration-deck topics + references.html (PDFs are image-only/no
              extractable text -> applied deck TITLES + references framework, not slide quotes; noted in doc §10).
              Classified 4 scenarios via the 6 canonical patterns: S1 SF->FRS RPI Fire-and-Forget (async, off-event
              Queueable callout to dodge callout-after-DML); S2 FRS->SF Remote Call-In (idempotent); S3 reusable
              logging = UI-Update-on-Data-Change (PE-backed, survives rollback); S4 Batch Data Sync = deferred fallback.
              Auth matrix: outbound Named/External Credential (client-creds/API key); inbound Connected App + least-priv
              integration user (JWT). Middleware seam documented at CalloutService (§7). Feeds 2.1/2.2/2.3 + ADR-002/005.
    [x] 2.1 - docs/requirements/functional-requirements.md (Logger/EventPublisher/CalloutService APIs; outbound; inbound; resilience)
              2026-06-16 DONE. 12 FRs w/ Given/When/Then, each traced to landscape scenarios S1-S3 + actor named
              (consumer-dev / platform / external FRS). A=API (FR1-4), B=outbound F&F (FR5-7), C=inbound Remote
              Call-In (FR8-9), D=cross-cutting (FR10-12). FR-9 idempotency + FR-11 200-record bulk are the
              "forces-correct-design" stories (idempotency = this project's bulkification analogue). Walking-skeleton
              subset = FR-1 + FR-4 + inbound half of FR-8. FR-12 (monitoring) + FR-7 alerting deferred to Phase 9.
              Scenario->FR coverage table closes traceability back to system-landscape.md.
    [x] 2.2 - docs/requirements/non-functional-requirements.md (integration security, idempotency, callout governor safety, observability, RTO)
              2026-06-16 DONE. 11 NFRs, each measurable target + named check + cites the FR(s)/scenario it constrains.
              NFR-1 security/secrets (Named Cred + least-priv integration user), NFR-2 data minimization, NFR-3
              idempotency (dup-delivery test), NFR-4 governor (no callout-after-DML, 200-rec <=50% limits), NFR-5
              resilience/RTO 30min replay, NFR-6 observability/SLO (>=99% success, p95), NFR-7 log durability
              (survives rollback), NFR-8 API stability (no breaking w/o major; deprecation), NFR-9 quality (>=85%),
              NFR-10 reproducibility (CONSUMES todo-app CI template = reuse proof), NFR-11 recoverability w/ consumers.
              NFR->FR coverage table proves every FR has >=1 measurable NFR. Feeds RTM (2.4).
    [x] 2.3 - docs/requirements/integration-contract.md (endpoints/schemas/auth/error-codes/idempotency-key with Vercel)
              2026-06-16 DONE. Concrete wire contract both sides build against independently. Outbound POST
              callout:FRS_Service/v1/notifications (F&F, 202 ack); inbound POST /services/apexrest/frs/v1/status
              (Remote Call-In, 200 applied|duplicate). Full status-code->SF-behavior + retry tables (which codes
              retry per NFR-5: 429/5xx/timeout yes; 400/401/403/404 no; 409=dedup success). Correlation id echoed
              across round-trip; idempotency key dedup'd by RECEIVER. Field dictionary w/ data classification
              (nothing > Internal crosses; Authorization=Restricted never logged). Worked FR-10 round-trip example.
              /v1 additive-only, breaking->/v2. Feeds Phase 4 mock + Phase 6 CalloutService/inbound + Phase 7 tests.
    [x] 2.4 - docs/requirements/rtm.md skeleton (100% of FR/NFR IDs)
              2026-06-17 DONE. 23 rows (12 FR + 11 NFR), all status=planned at Phase 2 close. Design/component/test
              forward-referenced to planned paths+phase (no blanks; integration-contract.md cited live). Same
              planned->designed->built->verified->frozen ladder as todo-app; frozen as v1.0.0 snapshot in Phase 8.
    [x] 2.5 - docs/requirements/risk-register.md (3rd-party downtime, secret leak, event-bus limits, replay storms)
              2026-06-17 DONE. 15 risks across study's 6 categories + new INTEGRATION category. Integration-specific
              (R-2 FRS downtime, R-3 replay storm, R-4 missing idempotency, R-10 contract drift) = the risks todo-app
              never faced -> justify Phase 8 integration-failure drill + Phase 7 contract/resilience tests. Every I=H
              risk (R-4/5/6/7/9/15) maps to a mandatory test or drill, not just a doc.
    NOTE (Phase 2 close 2026-06-17): Requirements baseline SET - changes now require a CCB entry. Discovery->FR->NFR
    ->contract->RTM->risks all cross-traced. NEXT = Phase 3 (architecture/diagrams/ADRs); 3.x design fills the RTM
    Design column + produces ADR-002/003/004/005, sequence diagrams, trust boundary, package dependency, public-api-spec.
[x] 3.0 - Phase 3 - Architecture and Diagrams  *** PHASE 3 COMPLETE 2026-06-17 ***
    [x] 3.1 - docs/architecture/integration-architecture.md (context; sync vs async; where middleware would sit)
              2026-06-17 DONE. Mermaid context graph (consumers -> base pkg -> FRS Vercel, both directions). Layer-approach
              table per direction; sync-vs-async rationale (only S2 inbound is sync, correctly); middleware seam at
              CalloutService (zero-consumer-change insertion point); observability spine via correlation id. Implements ADR-002/004.
    [x] 3.2 - docs/architecture/sequence-diagrams.md (outbound happy/timeout/retry; inbound happy/duplicate; poison replay)
              2026-06-17 DONE. 7 Mermaid sequenceDiagrams: §1 outbound happy (domain PE->trigger->Queueable->callout, user
              txn commits before FRS), §2 retry+backoff via Finalizer (same key), §3 exhaustion->dead-letter->replay (30min
              RTO), §4 inbound happy (JWT->idempotent apply), §5 duplicate->no-op (unique-constraint, race-safe), §6 logging
              survives rollback (immediate publish), §7 full round-trip one correlation id (FR-10). Encodes 1-enqueue/txn,
              same-key-on-retry, unique-upsert-dedupe, immediate-publish. Implements ADR-002/003/004/005.
    [x] 3.3 - docs/architecture/security-trust-boundary.md (Named Cred auth, secrets, least-priv inbound, InfoSec handoff artifact)
              2026-06-17 DONE. Trust-boundary Mermaid (2 controlled crossings); auth table per ADR-005; least-priv
              FRS_Integration perm set (API-only user, runAs-proven scope); secret/key/cert handling + rotation->Phase 8;
              data-in-transit + classification allowlist. §6 = InfoSec sign-off CHECKLIST (the handoff artifact).
    [x] 3.4 - docs/architecture/package-dependency.md (base <- todo-app consumer; install ordering)
              2026-06-17 DONE. Dependency graph (frs-platform base <- todo-app #1 + stub #2). sfdx-project.json
              dependencies[] example + version-pinning policy (pin major.minor; LATEST-in-minor safe via NFR-8). Install
              ordering seq (base before consumer) for release AND rollback-with-consumer-installed (NFR-11). Implements ADR-001.
    [x] 3.5 - docs/architecture/public-api-spec.md (the explicit API + stability guarantees)
              2026-06-17 DONE. THE public surface (api-governance §1): Logger (6 methods+Severity), EventPublisher (2),
              CalloutService (sendAsync + Request/Result), inbound /frs/v1/status + outbound /v1/notifications (-> contract).
              Stability legend (Stable/Beta/Internal); Log__e/Queueable/retry internals marked INTERNAL (hidden). Phase 7
              consumer-compat test PINS to this file = executable NFR-8. v1.0.0 frozen-surface summary.
    [x] 3.7 - RTM Design column FILLED + statuses bumped planned->designed
              2026-06-17 DONE. 20/23 rows now 'designed' (all design artifacts exist). 3 stay 'planned' by design:
              FR-12 (monitoring surface=Phase 9), NFR-9/NFR-10 (CI pipeline-design=Phase 4). No blanks anywhere.
    NOTE (Phase 3 close 2026-06-17): "design on paper first" rung COMPLETE - 5 ADRs + 5 architecture docs. Every
    integration decision (pattern/retry/idempotency/logging/auth/packaging) is recorded + diagrammed + traced before
    any code. NEXT = Phase 4 (delivery platform): base-pkg DX project, GitLab repo (USER: create sf-platform-enablement),
    .gitlab-ci.yml consuming todo-app CI template, build+deploy Vercel mock, secrets as masked CI vars, pipeline-design.md.
    [x] 3.6 - ADR-002-integration-pattern / ADR-003-retry-idempotency / ADR-004-log-architecture / ADR-005-auth-flow
              2026-06-17 DONE (4 ADRs, started Phase 3 with decisions before diagrams).
              ADR-002: S1=RPI Fire-and-Forget (domain PE -> trigger -> Queueable callout, bulk-safe); S2=Remote Call-In
              via custom Apex REST (idempotent, sync applied/duplicate); S3=UI-update PE logging. Excluded sync
              Request/Reply (callout-after-DML) + Data Virtualization; Batch Sync = fallback.
              ADR-003: outbound bounded retry(3) via Queueable re-enqueue w/ enqueueJob delay backoff + Finalizer ->
              Integration_DeadLetter__c -> ReplayService (same idempotency key). Inbound idempotency = Unique External Id
              field on Integration_Message__c (race-safe vs query-check/Platform-Cache). Retry config in custom metadata.
              ADR-004: Logger publishes Log__e w/ PUBLISH IMMEDIATELY (survives rollback=NFR-7) -> subscriber -> Log__c
              queryable; correlationId threads FR-10; Logger hides the event (public-API rule). DE PE-publish limit noted.
              ADR-005: outbound Named Cred + External Cred OAuth Client-Credentials (API-key fallback); inbound Connected
              App + JWT Bearer (cert, no stored secret - ties to arc-sf-jwt-bearer skill) as least-priv integration user.
              MFA/security-keys hardening; rotation = Phase 8 drill. Each flow justified by actor/secret-handling.
              ADRs already cited in RTM Design column; full RTM status bump (planned->designed) happens at rung 3.7
              once the diagrams + public-api-spec also land (ADRs are the decision half of the design, not all of it).
    [ ] 3.7 - Fill RTM design column for every FR/NFR
[ ] 4.0 - Phase 4 - Source Control, Orgs, Vercel Service, and CI
    [x] 4.1 - Scaffold base-package DX project (reuse API pin, .forceignore); register package in DevHub
              2026-06-17 PACKAGE REGISTERED: frs-platform = 0HogK00000038c9SAA in DevHub (00DgK00000QYu0qUAD - SAME DevHub
              as todo-app, confirmed: it holds todo-app pkg 0HogK0000002sM5SAI -> base<-consumer dependency will resolve).
              todo-app GOTCHA recurred: sf package create rewrote sfdx-project.json (default:false, wiped versionName/
              versionNumber/description) - REPAIRED (default:true, v0.1 / 0.1.0.NEXT / walking-skeleton desc). Scaffold below:
              2026-06-17 SCAFFOLD DONE (code half): sfdx-project.json (package frs-platform, no namespace), .forceignore,
              config/project-scratch-def.json (+enableHighVolumePlatformEvents for the PE-backed logging/eventing),
              package.json (test:unit --passWithNoTests since base is Apex-first), .gitignore, README.md, force-app
              skeleton w/ .gitkeep in classes/triggers/objects/permissionsets/platformEvents/namedCredentials/
              externalCredentials/customMetadata/lwc. API PINNED 66.0 to match the shared org farm (todo-app hit
              'v67 rejected' on same orgs); bump to 67 once orgs confirmed. REMAINING (needs Dev Hub): sf package create
              to register frs-platform -> populates packageAliases (separate 6/day pkg-build budget).
    [x] 4.2 - USER: create GitLab project sf-platform-enablement; protect main (MR-only) like sf-todo
              2026-06-17 DONE. Repo: https://gitlab.com/andysolomon/sf-platform-enablement (private). Nested git repo
              initialized in projects/platform-enablement/ (like sf-todo nests in projects/todo-app/), scaffold committed +
              pushed to main. main protected EXACTLY like sf-todo: push=No one, merge=Maintainers, only_allow_merge_if_
              pipeline_succeeds=true, merge_method=merge, remove_source_branch_after_merge=true. (GitLab auto-protected main
              with push=Maintainers on creation; re-protected to push=No one via DELETE+POST.) Push showed macOS keychain
              'failed to store: -61' (no-keychain session, harmless - push succeeded). First pipeline will be RED until
              4.1-register (sf package create) + 4.5 (CI auth vars) land - expected.
    [~] 4.3 - .gitlab-ci.yml that include:s the todo-app ci-templates/ (SECOND-CONSUMER validation - log any template gaps)
              2026-06-17 DRAFTED: consumes sf-todo template via cross-project include (project:/file:/ref:main - NOT local,
              since template lives in sf-todo repo). Sets SF_PACKAGE=frs-platform, SF_PERMSET=FRS_Integration. Thin jobs
              extend .sf-scan/.sf-lwc-test/.sf-apex-test/.sf-package/.sf-staging-install/.sf-prod-install + a CONSUMER-ADDED
              contract-test job (template has no integration testing). FINDING logged (NFR-10): cross-project ref:main
              couples to sf-todo default branch; proper fix = dedicated sf-ci-templates repo + tag ref. Can't run until repo
              exists (4.2) + orgs connected.
    [~] 4.4 - Build + deploy the Vercel mock service (REST out, webhook in, caller into SF) per integration-contract.md
              2026-06-17 BUILT + VERIFIED LOCALLY (deploy half remains). Lives at projects/frs-mock-service/ (separate from
              the SF DX repo per pipeline-design §3.4). Next.js 15 App Router, web-standard Request/Response handlers, jose
              for crypto. Endpoints implement integration-contract.md /v1: GET /v1/health; POST /oauth/token (OAuth client-
              credentials, ADR-005 outbound); POST /v1/notifications (Bearer-auth, contract validation w/ field allowlist,
              in-memory idempotency dedupe -> 202/409/400/401); POST /v1/simulate-callback (triggers S2 Remote Call-In to SF
              via JWT bearer, env-gated -> 503 if SF creds absent). `npm run build` GREEN (types+lint pass). Runtime smoke
              8/8 PASS: health 200, token issued, bad-creds 401, no-auth 401, valid 202, DUPLICATE 409, unknown-field 400,
              callback-no-creds 503. Tokens stateless HS256 (serverless-safe); idempotency in-mem per-instance (mock caveat;
              authoritative dedupe is SF-side NFR-3) - both documented. REMAINING: deploy to Vercel (needs Vercel auth) ->
              the deploy URL becomes the Named Credential endpoint; set env vars in Vercel project.
              2026-06-17 DEPLOYED to Vercel prod: project frs-mock-service, URL https://frs-mock-service-rezdny3pa-
              andrewsolomonedus-projects.vercel.app (● Ready, 32s build). Env vars set (encrypted, Production): FRS_CLIENT_ID
              =salesforce-platform, FRS_CLIENT_SECRET + FRS_TOKEN_SECRET (random; saved in frs-mock-service/.env.local
              gitignored - needed for the SF External Credential in Phase 6). BLOCKER: Vercel Deployment Protection is ON
              -> live URL returns 401 HTML auth wall, which would break SF callouts. USER ACTION: disable Vercel
              Authentication (dashboard: frs-mock-service > Settings > Deployment Protection > Vercel Authentication OFF),
              OR provide a VERCEL_TOKEN so I can PATCH ssoProtection=null via REST API. The mock's OWN OAuth still protects
              /v1/notifications, so disabling the Vercel wall is correct (not insecure). After OFF: re-run live smoke.
    [x] 4.5 - Store SFDX auth URLs (+ any deploy tokens) as masked CI vars (base64 lesson); integration secrets = ORG config not CI
              2026-06-17 DONE by REUSING sf-todo's values (Andy's call - same DevHub + same prod org). Sidesteps the CLI
              redaction entirely: GitLab returns masked VALUES to the owner via API, so copied repo->repo with
              glab variable get|set (values never printed). sf-platform-enablement now has SFDX_AUTH_URL_DEVHUB_B64
              (masked, unprotected) + SFDX_AUTH_URL_PROD_B64 (masked, protected) - flags mirror sf-todo. Both verified to
              decode to force://… (valid, not placeholders). Integration secrets (OAuth/JWT) stay ORG config per ADR-005, not CI.
    [ ] 4.5 - Store SFDX auth URLs (+ any deploy tokens) as masked CI vars (base64 lesson); integration secrets = ORG config not CI
    [x] 4.6 - docs/devops/pipeline-design.md (stages incl. integration-test job; Vercel deploy/verify)
              2026-06-17 DONE (written BEFORE the YAML per todo-app discipline). 7-job stage map; cross-project-include
              decision + NFR-10 FINDING; contract-test as consumer extension (don't edit shared template); secrets model
              (SFDX auth URLs in CI; OAuth/JWT secrets = org metadata per ADR-005; Vercel has own deploy); shared scratch
              budget; dependency-ordered release spans 2 pipelines (base here, consumer in todo-app repo).
    [ ] 4.7 - Verify gates: planted failing integration test goes red then green  (needs running pipeline)
    NOTE (Phase 4 partial 2026-06-17): code-side scaffold + pipeline design DONE without orgs/repo. BLOCKING USER STEPS to
    finish Phase 4: (4.2) create+protect GitLab project sf-platform-enablement; then I do (4.1-register) sf package create,
    (4.4) Vercel mock, (4.5) CI vars, (4.7) gate drill. sf CLI session-auth caveat applies (see project_sf_session_auth).
[x] 5.0 - Phase 5 - Walking Skeleton   *** v0.1.0 RELEASED TO PROD 2026-06-17 ***
    [~] 5.1 - Base pkg: Logger.log(String) -> one Platform Event/Log; consumer test
              2026-06-17 SOURCE AUTHORED + structurally validated (8 Apex files paired w/ meta, 19 XML well-formed; NOT yet
              deployed - holds shared scratch budget for execution). Log__e PE (publishBehavior=PublishImmediately, ADR-004)
              + Log__c queryable object (+ fields Message/Severity/Correlation_Id/Category) + LogEventSubscriber trigger ->
              LogEventHandler (@SuppressWarnings CRUD quarantine marker) persists Log__c. Logger.cls (inherited sharing;
              log/error/warn/info + Exception overload + category). LoggerTest: log_writesDurableRecord, survivesRollback
              (NFR-7), severityHelpers - via Test.getEventBus().deliver().
    [x] 5.2 - One outbound callout to Vercel via Named Cred (HttpCalloutMock unit test + real scratch-org call)
              2026-06-17 SOURCE AUTHORED: CalloutService.ping() GETs callout:FRS_Service/v1/health (no hard-coded URL),
              logs outcome. CalloutServiceTest uses HttpCalloutMock (200 + 500 cases, asserts endpoint = Named Cred path).
              *** 2026-06-29 LIVE-VERIFIED (FR-10) in scratch frs-fr10 ***: the Phase-6 outbound path
              CalloutService.sendAsync -> FRS_Service Named Cred -> External Credential FRS_Service_Cred (OAuth 2.0
              Client-Credentials) -> live token fetch from frs-mock-service.vercel.app/oauth/token -> authenticated
              POST /v1/notifications -> 202 accepted, receiptId returned, correlationId echoed (synchronous diag proof +
              async canonical smoke, 0 new dead letters). Dead-letter path also proven (pre-credential retries exhausted
              -> DL-0000000000). KEY FINDING: prodtest cannot host frs-platform - class-name collision with banking-app's
              frs-platform-core (both namespace-less, both define CalloutService) -> used a dedicated scratch org instead.
              ARTIFACTS PROMOTED TO SOURCE: externalCredentials/FRS_Service_Cred + namedCredentials/FRS_Service (secret-free,
              retrieved from the working org) + permissionsets/FRS_Outbound_Callout (External Credential Principal Access).
              The client_id/secret is the only non-metadata step (entered via Setup UI - Salesforce stores it write-only;
              both metadata deploy and ConnectApi.NamedCredentials.createCredential reject the obvious field names).
              Also fixed a latent contract-drift bug in scripts/apex/round-trip-smoke.apex (outbound payload used
              'salesforceRecordId'; mock allowlist is {externalRef,recordId,status} -> changed to 'recordId').
    [x] 5.3 - Vercel /health answers + logs inbound hit
              2026-06-17 DONE in 4.4 - mock GET /v1/health returns {status:ok,...}; verified in local smoke (8/8).
    [~] 5.4 - Package version 0.1.0.1 via CI, installed in prod; tag v0.1.0 + CHANGELOG.md
              2026-06-17 IN PROGRESS via MR !1 (feature/v0.1.0-walking-skeleton -> main, MERGED). 4.7 GATE DRILL DONE:
              planted SOQL-injection -> MR scan RED (apex-test SKIPPED at validate = 0 scratch) -> reverted -> GREEN.
              Gate drill also caught a 2nd real bug: package.json test:unit had `sfdx-lwc-jest --passWithNoTests` (missing
              `--`); lwc-test RED -> fixed to `sfdx-lwc-jest -- --passWithNoTests` (the todo-app gotcha). Also added
              package-lock.json (CI npm ci needs it) + CHANGELOG [0.1.0]. MR merged green (scan/lwc/apex/contract all pass).
              MAIN PIPELINE: scan/lwc-test/apex-test/contract-test PASS, **version PASS -> package 0.1.0.1 BUILT**,
              staging-install FAILED, prod-install skipped. FINDING (NFR-10 template-fit): the template's staging-install
              runs `npx playwright test` UNCONDITIONALLY -> "No tests found" exit 1, because this headless base package has
              no UI/e2e at v0.1.0 (todo-app had a UI). Package itself installed fine in staging; only the e2e step failed.
              FIX OPTIONS (decision pending): (a) add a labeled placeholder e2e + playwright.config so the gate passes
              until Phase 7 real round-trip e2e; (b) propose template opt-out for headless pkgs. BUDGET WALL: 5/6 scratch
              creates used today (2 validation + MR apex-test + main apex-test + main staging-install); finishing needs a
              new release pipeline = apex-test(1)+staging-install(1) = 2 more -> NEXT BUDGET WINDOW. prod-install (manual,
              needs green staging-install) + tag v0.1.0 therefore DEFERRED to next window.
              E2E FIX MERGED (MR !2, 2026-06-17, 0 scratch): added playwright.config.js + e2e/smoke.e2e.js (labeled
              placeholder; named *.e2e.js so sfdx-lwc-jest doesn't sweep it - todo-app jsdom gotcha avoided; e2e files
              not code-gated so MR ran scan+contract-test only). main is now RELEASE-READY.
              >>> RESUME NEXT BUDGET WINDOW: run `glab ci run -b main -R andysolomon/sf-platform-enablement` (web-triggered
              pipeline => changes-rules evaluate true => full release pipeline runs: apex-test + version(new 0.1.0.2) +
              staging-install[placeholder e2e now GREEN]). Then trigger the manual prod-install job (Release-Manager gate),
              verify prod install, tag v0.1.0 + push tag, then 5.5 smoke checklist + RTM rows -> verified. Budget needed: 2
              scratch (apex-test + staging-install). NOTE: package 0.1.0.1 already built today; the rerun builds 0.1.0.2 -
              fine (immutable versions; tag the one that reaches prod).
              *** DONE 2026-06-17 (same day, budget actually allowed it) ***: DailyScratchOrgs showed 3 remaining (my 5/6
              estimate was wrong - real was 3 used). Triggered `glab ci run -b main` (web source -> changes-rules true ->
              full release pipeline). ALL JOBS GREEN incl staging-install (placeholder e2e passed). version built pkg
              0.1.0.2 (04tgK000000DsNZQA0). prod-install MANUAL gate APPROVED by Andy (Release-Manager hat) -> installed to
              prodtest. Tag v0.1.0 pushed (commit 1b5ec90). Pipeline 2609367709.
    [x] 5.5 - Smoke-test checklist recorded; RTM skeleton rows verified
              2026-06-17 DONE. docs/release/smoke-test-v0.1.0.md: (1) pkg installed in prod ✅ (2) Logger runs in prod ✅
              (3) Log__c PERSISTED in prod (SMOKE-v0.1.0 row) ✅ - proves Logger->LogEvent__e(publish-immediate)->subscriber
              ->Log__c round-trip works in PROD (FR-1/FR-2/NFR-7 verified). RTM: FR-1 + FR-2/NFR-7 -> verified; FR-4 + FR-8
              -> built (live callout/inbound deferred to Phase 6). UI sign-off n/a (headless pkg).
    PREP NOTE (2026-06-17): also wrote FrsStatusResource (inbound FR-8 skeleton: 200 applied + logs echoed correlation id)
    + test; FRS_Integration least-priv perm set (FrsStatusResource class access + Log__e create only); scripts/apex/
    seed-integration.apex (SF_SEED_SCRIPT); docs/devops/named-credential-setup.md (execution runbook for the credential).
    *** VALIDATED ON SCRATCH ORG 2026-06-17 (2 scratch creates used, cleaned up) ***
    Deploy GREEN + Apex tests 7/7 PASS + Org-Wide Coverage 96% (Logger 95 / FrsStatusResource 94 / others 100 - all >85 gate).
    survivesRollback test PASSES -> publish-immediate + Database.rollback semantics CONFIRMED in Apex test context (NFR-7
    de-risked). API 66.0 ACCEPTED by orgs. Code Analyzer could NOT run locally (PMD/CPD/SFGE engines need Java engine init
    that failed this session) -> runs in CI (template installs JRE). 3 WATCH items: (a) survivesRollback CONFIRMED, (b) API66
    CONFIRMED, (c) analyzer DEFERRED-to-CI.
    TWO REAL FINDINGS the scratch validation caught (interview gold):
    (1) **base-name namespace collision**: Log__e (platform event) and Log__c (custom object) CANNOT coexist - both have
        base name "Log" -> "That object name is already in use". FIX: renamed event Log__e -> LogEvent__e (frees "Log" for
        Log__c). Updated Logger/LogEventHandler/LogEventSubscriber/permset + all 7 docs. Custom objects, platform events,
        custom metadata, big objects share one base-name namespace per org.
    (2) [CORRECTED] What looked like "PE-before-Apex ordering" was actually a CASCADE from finding #1: when Log__e failed
        to deploy (collision), Apex referencing it showed "Invalid type". POST-RENAME a single combined deploy SUCCEEDS -
        confirmed via dry-run to DevHub (20 components, 0 failures, 0 scratch). So the CI template's single-pass deploy is
        FINE; no two-pass workaround needed. Lesson: don't generalize a cascade symptom into a separate root cause without
        isolating it (a good debugging discipline note).
    Also fixed: scratch def had invalid platformEventSettings.enableHighVolumePlatformEvents (removed - high-volume is
    default); permset Create-LogEvent__e required Read-LogEvent__e (added allowRead).
    EXECUTION ORDER once toggles done: feature branch -> MR (gate drill 4.7: plant failing test red->green; mind 2-pass
    deploy) -> set up Named Credential (named-credential-setup.md) -> live ping (5.2) -> merge -> package version (5.4) ->
    staging -> prod -> tag v0.1.0. Code is PROVEN deployable+green; only the live callout + CI/prod release remain.
[~] 6.0 - Phase 6 - Core Platform Components & Bidirectional Integration
    *** 2026-06-23: MR !3 (6-A outbound + 6-B inbound + 6-C prep) MERGED to main. main release pipeline running
        (version builds frs-platform 0.2.0 + staging-install); prod-install is the MANUAL gate (Andy, charter SoD).
        CI debugging FINDINGS (interview-gold, all external to the Phase 6 code):
        (1) scan RED on 5 High ApexCRUDViolation in the new platform-infra classes -> fixed with the v0.1.0
            convention: class-level @SuppressWarnings('PMD.ApexCRUDViolation') + ApexDoc rationale (greppable
            quarantine marker). CalloutService/IdempotencyGuard/ReplayService/RetryPolicy.
        (2) apex-test RED: `sf org login sfdx-url` crashed ("Detected unsettled top-level await ... await
            cli.run()") -> ROOT CAUSE = the shared sf-todo template floats SF_CI_IMAGE=node:22, which drifted to
            22.23.0 and breaks the pinned CLI 2.137.7 login. THE NFR-10 cross-project coupling risk (ref: main +
            floating image) MATERIALIZED. FIX = pin SF_CI_IMAGE=node:20 consumer-side in .gitlab-ci.yml (lwc-test
            passed on node:22 because it never calls sf login). Real remediation still owed: template -> prebaked/
            pinned image (pipeline-design 3.1). A RED HERRING en route: refreshed the (5-day-old) DevHub auth URL
            CI var first (secret-safe: sf org auth show-sfdx-auth-url -o DevHub | base64 | glab variable set,
            never printed; note glab variable set only CREATEs -> delete then set to update) — harmless but not
            the cause.
        (3) LIMIT_EXCEEDED on scratch create: DevHub ActiveScratchOrgs is 3 concurrent and all 3 were held by
            OTHER projects (airline-dev/hotel-booking/sfra-dev). `sf limits api display` cols are REMAINING|MAX
            (0|3 = full, not empty — earlier misread). Freed one slot by deleting airline-dev (Andy authorized;
            don't delete orgs you didn't create without asking). DailyScratchOrgs (6/day) is a SEPARATE limit.
        MR pipeline = scan/lwc-test/apex-test/contract-test only; version/staging/prod are release-gated to main. ***
    *** 2026-06-23 RELEASED v0.2.0: MR !4 bumped versionNumber -> 0.2.0.NEXT (api-governance minor bump) -> main
        pipeline built frs-platform 0.2.0.1 (04tgK000000DvN3QAK, 97% cov) -> staging-install GREEN -> prod-install
        (Andy-authorized) INSTALLED to prodtest. Tag v0.2.0 pushed. INBOUND LIVE-VERIFIED in prodtest against 0.2.0.1
        (anonymous apex: apply=applied, replay=duplicate, recordCount=1 -> FR-8/FR-9 proven in prod, record cleaned up).
        RTM: FR-3/4/5/6/7/8/9/11 + NFR-3/4/5 -> verified (CI-green; FR-8/9 also live). FR-10 -> built (full live
        round-trip still needs the OUTBOUND callout = FRS_Service Named Credential, sandbox-blocked for the agent;
        inbound JWT/Connected App optional since logic is proven). Phase 6 FUNCTIONALLY COMPLETE + SHIPPED. ***
    [ ] 6.1 - Logger/error framework (Platform-Event-backed, survives rollback; severity; correlation id)
    [x] 6.2 - Generic EventPublisher wrapper (testable)   *** Phase 6 STARTED 2026-06-17; MR 6-A foundation ***
              2026-06-17 DONE + validated on scratch (100% class cov, 97% org-wide, 10 tests pass). EventPublisher.publish
              (single + List<SObject>) over EventBus.publish; failures surfaced (returned) + logged via Logger (FR-3).
              Failure branch made testable: extracted @TestVisible logFailures(); test fabricates a failed
              Database.SaveResult via JSON.deserialize (no public ctor) — the standard pattern.
              FINDINGS this story: (1) EventBus.publish returns SUCCESS even for a no-permission user — PE publish does NOT
              surface create-perm failure as a failed SaveResult (delivery failures happen later), so the perm-less-user
              approach to cover the branch DOESN'T work; JSON-deserialized SaveResult does. (2) Log__c.Message__c is a
              LongTextArea -> NOT filterable in SOQL WHERE (can't `Message__c LIKE`); assert on Severity__c instead.
              (3) dry-run to DevHub (0 scratch) is the cheap compile-check; reserve scratch for test runs (burned 4 scratch
              iterating today -> 2 left; be economical). Phase 6 PLAN: 3 MRs — 6-A outbound framework (EventPublisher[done]
              + CalloutService async/retry/dead-letter + bulk), 6-B inbound idempotency, 6-C live round-trip + todo-app
              consumer (gated on Vercel-protection-OFF + Named Credential + Connected App/JWT).
    [x] 6.3 - Outbound CalloutService (Named Cred + External Services; retry/backoff; async; bulk-safe)
              2026-06-18 DONE + validated on scratch ws-p6e: full local suite 26/26 pass, 97% org-wide; new classes
              CalloutFinalizer 100%, CalloutService 100%, ReplayService 100%, RetryPolicy 96%, CalloutQueueable 89%
              (the one uncovered line is the Test.isRunningTest()-guarded System.enqueueJob retry-chain — UNAVOIDABLE:
              a Queueable cannot chain another Queueable in test context). MR 6-A COMPLETE.
              ARCHITECTURE (ADR-003): CalloutService.sendAsync(Request|List<Request>) is the public surface (no
              endpoint/secret — routes via callout:FRS_Service Named Credential; Idempotency-Key + X-Correlation-Id
              headers). Bulk-safe: chunk() splits into <=100/txn (FR-11) and enqueues one CalloutQueueable per chunk.
              CalloutQueueable (Database.AllowsCallouts) does the callout, then RetryPolicy.decide(status,attempt) ->
              SUCCESS/RETRY/DEAD_LETTER: retry re-enqueues attempt+1 with exponential backoff (1->2->4 min, capped at
              the 10-min enqueueJob ceiling); terminal 4xx dead-letters immediately; transient 5xx/429/timeout(status 0)
              retries until maxAttempts then dead-letters. CalloutFinalizer = safety net: dead-letters carried work if the
              job dies with an UNHANDLED_EXCEPTION (finalizers run even when the Queueable throws). Integration_DeadLetter__c
              (DL-autonumber) captures correlation id, idempotency key, payload, cred path, method, last error/status,
              attempt count, Status NEW/REPLAYED/RESOLVED. ReplayService.replay(dlId) re-sends with the SAME idempotency
              key (FRS dedupes a re-applied message; 409=success) and marks REPLAYED (FR-7). RetryPolicy is the pure,
              fully-unit-tested decision core (isRetryable/backoffMinutes/maxAttempts/decide) — deliberately separated from
              the async machinery (the testable seam). New permset FRS_Operations (operator: read DLs + edit Status +
              ReplayService) — SEPARATE from FRS_Integration (inbound, least-priv) per charter SoD.
              FINDINGS this story:
              (1) global signatures can't carry a non-global (public) type: `global` ctor/method with a
                  List<CalloutService.WorkItem> param FAILS compile ("global methods do not support parameter type ...").
                  Fix = make the INTERNAL infra classes (CalloutQueueable/CalloutFinalizer) `public` not `global` (correct
                  per api-governance: they're ⚫ internal, never consumer API) rather than leaking WorkItem to global.
              (2) Custom Metadata RECORD (.md-meta.xml) deploy throws opaque UNKNOWN_EXCEPTION (-2086602991) on this org —
                  BOTH in check-only dry-run AND a real scratch deploy, with the TYPE already present, and regardless of
                  3.0 vs 3 value form (numberComponentErrors=0, no clue which component). The TYPE + fields deploy clean.
                  RESOLUTION: REMOVED the Default record from source (a non-deployable file in force-app would break the
                  package-version build, which deploys all of force-app). RetryPolicy.maxAttempts() falls back to constant 3
                  (= the intended Default) when no record exists, so behavior is identical. Integration_Setting__mdt type is
                  retained (proves config-not-code capability + deploys clean); seed a Default record via Setup UI if tuning
                  is ever needed. NOTE: a CMDT record in a check-only deploy ALSO masks all other component errors as the
                  same UNKNOWN_EXCEPTION — temporarily stash customMetadata/ to surface real compile errors in a dry-run.
              (3) dry-run --test-level NoTestRun -o DevHub is the 0-scratch compile gate; DEPLOYS to an EXISTING scratch
                  are FREE (no scratch-create cost) — iterate freely there; only `org create scratch` spends daily budget.
              BUDGET: created 1 scratch today (ws-p6e); DailyScratchOrgs was 2 remaining -> 1 left.
    [x] 6.4 - Inbound path (Vercel -> SF Platform Event/REST; IDEMPOTENT apply by key)
              2026-06-18 DONE + validated on ws-p6e (free deploy): full suite 35/35 pass, 98% org-wide;
              FrsStatusResource 98%, IdempotencyGuard 100%. MR 6-B COMPLETE (FR-8 receipt/validate/auth half + FR-9).
              ARCHITECTURE (ADR-003 idempotency): Integration_Message__c (MSG-autonumber) with Idempotency_Key__c =
              UNIQUE External Id (required) is the dedupe store. IdempotencyGuard.reserve(Message) INSERTs it; a second
              insert with the same key fails DUPLICATE_VALUE -> caught -> Outcome.DUPLICATE. This is platform-enforced
              uniqueness => correct even under CONCURRENT duplicate delivery, where query-then-insert RACES (both readers
              see "absent", both apply) — the failure mode the todo-app never had to face. Reservation shares the inbound
              transaction, so a downstream failure rolls back the reservation too (safe re-apply). FrsStatusResource
              rewritten: parse (JSON.deserialize to typed StatusRequest/Payload) -> validate (400 on malformed/empty JSON
              or missing idempotencyKey/payload.salesforceRecordId, BEFORE any side effect) -> reserve -> 200 applied |
              200 duplicate (deduped, no second effect, logged) per contract §4.2/4.3. StatusResult now {status, recordId,
              correlationId}. Correlation id: body wins, header X-Correlation-Id fallback (FR-10 echo). FRS_Integration
              permset extended: Integration_Message__c Create+Read + FLS (Idempotency_Key__c omitted from FLS — a
              universally-required field can't carry FLS).
              SCOPE NOTE: at the platform layer the durable Integration_Message__c IS the "applied" effect; mutating a
              CONSUMER's business record (todo-app Todo) is the consumer's concern -> wired in 6-C/6.6. DEFERRED (noted):
              strict unknown-field allowlist rejection (contract §5 over-sharing 400) — current parse is lenient
              (JSON.deserialize ignores unknown fields); use deserializeStrict + dictionary check when 6-C lands.
              BUDGET: 0 scratch created (reused ws-p6e, free deploys) -> still 1 DailyScratchOrg left today.
    [ ] 6.4b - Strict inbound field-allowlist (contract §5): reject unknown payload fields with 400 (deferred from 6-B)
    [~] 6.5 - Full round-trip (todo complete -> event -> callout -> webhook back -> SF update; correlation chain logged)
              2026-06-18 ARTIFACTS PREPPED (live execution GATED). Authored (no deploy/scratch):
              docs/release/round-trip-test-plan-v0.2.0.md (A outbound / B inbound / C full-round-trip FR-10 checklist +
              dead-letter+replay + dedup exit criteria) and scripts/apex/round-trip-smoke.apex (executable post-setup
              smoke: outbound sendAsync + inbound handleStatus applied->duplicate, prints the correlation-trace SOQL).
              GATED ON LIVE SETUP (3 human steps): (1) Vercel Deployment Protection OFF; (2) Named/External Credential
              FRS_Service (docs/devops/named-credential-setup.md); (3) Connected App + JWT (NEW: docs/devops/
              connected-app-jwt-setup.md — full inbound runbook: openssl cert gen, integration user + FRS_Integration,
              Connected App JWT config, token exchange, verify curl + committable ConnectedApp template). Plus published
              frs-platform >=0.2.0 installed.
    [~] 6.6 - todo-app consumes the base package (dependency declared; calls Logger/EventPublisher)
              2026-06-18 CONSUMER WIRING PREPPED (ready to apply, GATED on published pkg). Authored
              docs/integration/consumer-onboarding.md: sfdx-project.json dependencies block (frs-platform@0.x alias +
              04t id), sf package install, External Credential Principal Access, and a complete sample consumer patch —
              TodoTriggerHandler.notifyFrsOnCompletion() builds CalloutService.Request on Open->Done (stable idempotency
              key todo-<id>-completed, fresh correlation id, todo.completed body per contract §3.1) -> CalloutService.
              sendAsync(List) (bulk-safe) + Logger.info. Demonstrates the thesis: consumer writes ~25 lines of intent,
              gets retry/backoff/dead-letter/idempotency/bulk/secret-free-callout/correlated-observability for free.
              NOT deployed to todo-app/force-app (would break its build until frs-platform is installed) — kept as a
              ready-to-apply doc per the base<-consumer model (ADR-001). Decision recorded: 6-C path = "prep artifacts now"
              (Andy), live wiring in a later dedicated session.
    [ ] 6.7 - Per-MR tests incl. idempotency + 200-record bulk callout path; coverage >=85%; RTM updated
[ ] 7.0 - Phase 7 - Test Strategy Expansion
    [ ] 7.1 - HttpCalloutMock suites (timeout/4xx/5xx/malformed)
    [ ] 7.2 - Platform Event harness via Test.getEventBus().deliver()
    [ ] 7.3 - Contract tests against deployed Vercel mock (in CI)
    [ ] 7.4 - Resilience + idempotency tests (retry exhaustion -> poison; duplicate -> no-op)
    [ ] 7.5 - Consumer-compatibility test (public API surface guard = deprecation policy made executable)
    [ ] 7.6 - Playwright e2e for the headline round-trip
    [ ] 7.7 - docs/testing/test-strategy.md + test-data-strategy.md; RTM test column 100%
[ ] 8.0 - Phase 8 - Release Management and Operations
    [ ] 8.1 - release-runbook.md (dependency-ordered base->consumer install)
    [ ] 8.2 - rollback-runbook.md + REHEARSAL (rollback of a package that has consumers; additive policy)
    [ ] 8.3 - integration-failure runbook + LIVE DRILL (Vercel down/timeout/poison -> detect/retry/replay) <- earns its keep
    [ ] 8.4 - breaking-change/deprecation DRILL (base v2 evolves API; todo-app consumer survives upgrade)
    [ ] 8.5 - secrets-rotation-runbook.md + ROTATION DRILL (zero-downtime Named Cred/API key rotation)
    [ ] 8.6 - salesforce-release-readiness.md (seasonal impact on integration/API surface)
    [ ] 8.7 - post-release-review-template.md + v1.0.0 retro
    [ ] 8.8 - Release v1.0.0 (full set; tag; changelog; RTM frozen)
[ ] 9.0 - Phase 9 - Platform Deepening: Observability, Reuse, Hardening
    [ ] 9.1 - Observability surface (LWC/report over log events: success rate, latency, failures) = monitoring
    [ ] 9.2 - Alerting on integration failure (flow/email/event on N failures) = alerting
    [ ] 9.3 - docs/devops/platform-metrics.md (integration SLOs + DORA from real history)
    [ ] 9.4 - Second consumer stub adopts base API with ZERO base changes (proof of reuse)
    [ ] 9.5 - docs/architecture/backup-and-encryption.md (Shield design + encrypted fields + backup)
    [ ] 9.6 - docs/devops/consumer-onboarding.md (zero-to-consuming; time yourself)

DEFERRED: real middleware (MuleSoft), Data Cloud, paid Shield/SSO (design-only), load testing, real SAFe ceremonies.
